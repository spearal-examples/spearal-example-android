<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/menu_url"
        android:icon="@android:drawable/ic_menu_more"
        android:title="@string/menu_url"
        android:showAsAction="ifRoom">
        <menu>
            <item android:id="@+id/menu_url1"
                android:title="Example v1"/>
            <item android:id="@+id/menu_url2"
                android:title="Example v2"/>
        </menu>
    </item>

    <item android:id="@+id/menu_new"
        android:alphabeticShortcut="+" 
        android:title="@string/menu_new"
        android:icon="@android:drawable/ic_menu_add"
        android:showAsAction="ifRoom"/>
</menu>
